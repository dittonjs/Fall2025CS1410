public class Simulator {
    boolean[][] world;

    public Simulator(int sizeX, int sizeY) {
        world = new boolean[sizeY][sizeX];
    }
}

/**
 Plane has a Cockpit // composition
 Course has Student(s) // aggregation
 Account uses a FileWriter // association
 ObjectA has ObjectB has ObjectC has Object...Z
 */





